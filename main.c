#include "holberton.h"
#include <limits.h>

int main()
{
        _printf("%i", INT_MAX);
        _printf("%i", INT_MIN);
        _printf("%d", INT_MAX);
        _printf("%d", INT_MAX);

        return (0);
}
